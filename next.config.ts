/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["images.pexels.com","www.shutterstock.com","dtwhistledev.wpengine.com","cdn.pixabay.com"],
  },
};

export default nextConfig;
